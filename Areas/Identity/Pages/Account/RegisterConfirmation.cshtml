@page
@model RegisterConfirmationModel
@{
    ViewData["Title"] = "Confirmación de registro";
}

<h1>@ViewData["Title"]</h1>

<div class="alert alert-success" role="alert">
    <h4 class="alert-heading">¡Gracias por registrarte!</h4>
    <p>Hemos enviado un correo electrónico a <strong>@Model.Email</strong> con un enlace de confirmación.</p>
    <hr>
    <p class="mb-0">Por favor, revisa tu bandeja de entrada (y la carpeta de spam) y haz clic en el enlace para activar tu cuenta.</p>
</div>

<div class="mt-4">
    <p>Una vez que confirmes tu correo electrónico, podrás iniciar sesión en nuestra plataforma.</p>
    
    <form method="post">
        <input type="hidden" name="Email" value="@Model.Email" />
        <button type="submit" class="btn btn-link p-0">¿No recibiste el correo? Haz clic aquí para enviarlo nuevamente</button>
    </form>
    
    <div class="mt-3">
        <a class="btn btn-outline-primary" href="/Identity/Account/Login">Volver a inicio de sesión</a>
    </div>
</div>

@if(!string.IsNullOrEmpty(Model.StatusMessage))
{
    <div class="alert alert-info mt-3">
        @Model.StatusMessage
    </div>
}